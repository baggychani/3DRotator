[project]
name = "3drotator"
version = "0.1.0"
description = "Local image 3D rotator and PNG exporter for social story assets."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
